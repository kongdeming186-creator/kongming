# 部署指南 - 让其他电脑访问

## 方式一：Netlify 拖拽部署（最简单）⭐

**无需任何配置，拖拽上传即可！**

1. 打开网站：https://app.netlify.com/drop
2. 注册/登录 Netlify（可用 GitHub 账号登录）
3. 将 `dist` 文件夹直接拖拽到网页上
4. 等待上传完成，自动获得一个网址如：`https://xxx.netlify.app`
5. 这个网址任何人都能访问！

---

## 方式二：Vercel 部署

1. 打开网站：https://vercel.com
2. 注册/登录 Vercel（可用 GitHub 贷号登录）
3. 点击 "New Project" → "Import Git Repository" 或直接上传
4. 部署完成后获得网址：`https://xxx.vercel.app`

---

## 方式三：GitHub Pages 部署

**前提条件：**
- 有 GitHub 账号
- 已安装 Git

**步骤：**

### 1. 在 GitHub 创建新仓库
1. 登录 GitHub
2. 点击右上角 "+" → "New repository"
3. 仓库名如：`liujiao-street-system`
4. 选择 "Public"（公开）
5. 点击 "Create repository"

### 2. 初始化 Git 并推送
在项目目录执行以下命令：

```bash
# 初始化 Git
git init

# 添加所有文件
git add .

# 提交
git commit -m "初始化项目"

# 连接远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/liujiao-street-system.git

# 推送
git push -u origin main
```

### 3. 启用 GitHub Pages
1. 进入仓库页面
2. 点击 "Settings" → "Pages"
3. Source 选择 "Deploy from a branch"
4. Branch 选择 "main"，目录选择 "/ (root)"
5. 点击 "Save"

### 4. 添加部署脚本（可选）
在仓库创建 `.github/workflows/deploy.yml`：

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: npm install
      
      - name: Build
        run: npm run build
      
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
```

---

## 方式四：上传到任意静态服务器

将 `dist` 文件夹上传到以下任意服务器：

- 阿里云 OSS
- 腾讯云 COS
- 七牛云
- 自己的 Nginx/Apache 服务器

---

## 推荐：Netlify（最简单）

**为什么推荐 Netlify？**
- 完全免费
- 无需安装 Git
- 拖拽上传即可
- 自动分配域名
- 支持 HTTPS
- 全球 CDN 加速

**步骤：**
1. 打开 https://app.netlify.com/drop
2. 用 GitHub 登录
3. 把 `d:\原型\dist` 文件夹拖上去
4. 完成！获得永久可访问的网址